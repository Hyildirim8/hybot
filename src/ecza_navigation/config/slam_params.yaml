# slam_params.yaml — SLAM Toolbox config for ecza-robotu.
#
# Mode: online_async — builds map in real time as the robot moves.
# Switch to localization mode after saving a map (see nav2_navigation.launch.py).
# ---------------------------------------------------------------------------

slam_toolbox:
  ros__parameters:
    # Plugin/mode
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # Frames
    odom_frame: odom
    map_frame: map
    base_frame: base_link     # controller publishes odom→base_link on /tf
    scan_topic: /scan
    use_map_saver: true
    mode: mapping        # mapping | localization

    # Pose graph
    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.02   # 50 Hz TF updates
    map_update_interval: 1.0
    resolution: 0.05
    min_laser_range: 0.15
    max_laser_range: 10.0
    minimum_time_interval: 0.15      # 0.10 çok sık keyframe → kötü odom ile birleşince harita bozuluyor
    transform_timeout: 1.0           # RPi için daha fazla TF toleransı
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000

    # Loop closure
    enable_interactive_mode: true
    use_scan_matching: true
    use_scan_barycenter: false
    minimum_travel_distance: 0.25    # 0.15 → 0.25: daha az ama kaliteli keyframe
    minimum_travel_heading: 0.30     # 0.20 → 0.30: ~17° dönüşte yeni keyframe (daha kararlı)
    scan_buffer_size: 10             # 20 → 10: eski kötü odom'lu scan'ler birikmesin
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.45   # 0.30 → 0.45: zayıf eşleşme reddedilsin
    distance_variance_penalty: 1.0   # 0.8 → 1.0: kötü odometry'ye daha az güven
    angle_variance_penalty: 2.0      # 1.6 → 2.0: açısal odom hatasına daha az güven
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.524
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 1.2
    minimum_distance_penalty: 0.5
    use_response_expansion: true
